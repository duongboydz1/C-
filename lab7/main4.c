#include <stdio.h>
#include <stdlib.h>

void main()
{
int count, num, num1 = 1, num2 = 1, so_tiep_theo, i ;
printf("so luong cac so trong chuoi:");
scanf("%d", num);
for(i = num1; i <= num; i++)
{
printf("%d", i);
}
}
