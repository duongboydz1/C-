#include <stdio.h>
void main()
{
	char name[20];
	/* nam is declared as a single dimentional character array */
	/* clears the screen */
	
	puts("Enter your name:");
	gets(name);
	
	/*Dissplay message */
	gets(name);
	
	/*Accepts the input*/
	puts("Hi there:");
	
	puts(name);
	
	/*Dissplay message */
	getch();
}
